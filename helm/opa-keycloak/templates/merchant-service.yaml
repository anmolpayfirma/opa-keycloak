{{- if .Values.merchant_service.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: merchant-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: merchant-service
    version: v1
spec:
  replicas: {{ .Values.merchant_service.replicas | default 1 }}
  selector:
    matchLabels:
      app: merchant-service
      version: v1
  template:
    metadata:
      labels:
        app: merchant-service
        version: v1
    spec:
      containers:
      - name: merchant-service
        image: "{{ .Values.merchant_service.image.repository }}:{{ .Values.merchant_service.image.tag }}"
        ports:
        - containerPort: 8080
        resources:
          {{- toYaml .Values.merchant_service.resources | nindent 10 }}
        env:
        - name: PORT
          value: "8080"
        livenessProbe:
          httpGet:
            path: /api/v1/merchants/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/v1/merchants/health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: merchant-service-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: merchant-service
spec:
  selector:
    app: merchant-service
  ports:
  - port: 8080
    targetPort: 8080
    name: http
{{- end }}
